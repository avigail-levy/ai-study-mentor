services:
  db:
    image: pgvector/pgvector:pg16
    container_name: postgres-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: vector_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/db/vector_db.sql:/docker-entrypoint-initdb.d/vector_db.sql:ro
    # בדיקה שה-DB באמת מוכן
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d vector_db"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - ai-network
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: finalaiproject-backend-1
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    depends_on:
      db:
        condition: service_healthy
    networks:
      - ai-network
    # שינוי ל-node ישיר כדי למנוע בעיות nodemon בדוקר
    command: node server.js
    restart: always

  frontend:
    build: ./frontend
    container_name: finalaiproject-frontend-1
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - ai-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  ai-network:
    driver: bridge